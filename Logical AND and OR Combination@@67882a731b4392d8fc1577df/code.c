#include <stdio.h>

int main(){
    int a,b;
    scanf("%d %d",&a,&b);
     if ((num1 > 0 && num2 < 0) || (num1 == 0 && num2 == 0)) {
        printf("True");
    }else{
        printf("False");
    }
    return 0;
}